---
title: 'List Knowledge Bases'
openapi: 'GET /api/v1/knowledge-bases/'
---

Retrieve all knowledge bases created by the authenticated user (this endpoint is scoped to the creator, not shared company-wide).

<CodeGroup>
```bash cURL
curl -X GET "https://api-be.atthene.com/api/v1/knowledge-bases/" \
  -H "x-api-key: YOUR_API_KEY"
```

```python Python
import requests

response = requests.get(
    "https://api-be.atthene.com/api/v1/knowledge-bases/",
    headers={"x-api-key": "YOUR_API_KEY"}
)
knowledge_bases = response.json()
```

```javascript JavaScript
const response = await fetch('https://api-be.atthene.com/api/v1/knowledge-bases/', {
  headers: {
    'x-api-key': 'YOUR_API_KEY'
  }
});
const knowledgeBases = await response.json();
```
</CodeGroup>

<Warning>
This endpoint does not currently support `status` or `processing_engine` query-parameter filtering - it always returns every knowledge base you created. Filter client-side on the `status`/`processing_engine` fields in the response if needed.
</Warning>

## Response

```json
[
  {
    "id": "kb_123",
    "name": "Product Knowledge Base",
    "description": "Product documentation and guides",
    "processing_engine": "milvus",
    "status": "completed",
    "progress": 100,
    "datasource_count": 2,
    "success_rate": 100,
    "created_by_name": "John Doe",
    "created_at": "2025-01-15T10:30:00Z",
    "updated_at": "2025-01-15T10:35:00Z"
  }
]
```
